#暂不支持redis
scindapsus:
  calcite:
    schemas:
      - name: json
        file:
          dir: json
      - name: csv
        file:
          dir: csv
      - name: my
        jdbc:
          driver: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://localhost:3306/ds1
          user: root
          password: root